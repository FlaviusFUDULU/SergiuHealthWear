@model IEnumerable<AplicatieSalariati.Models.TaxePrestabiliteModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Impozit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CAS)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Somaj)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sanatate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Impozit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CAS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Somaj)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sanatate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
